# DONE — Rapprochement Bancaire

Tâches validées (APPROVE). Détail archivé dans `DONE_DETAIL/`.

| Task | Domaine | Sujet | Validé le |
|------|---------|-------|-----------|
| TASK-061 | Sécurité / Architecture | **Intégration du moteur de licence `GRLicence`** (demande PO 2026-07-20, lecture seule contre `ApLicence.Server`, protocole REQUEST, aucun siège consommé). Package NuGet local (`nuget.config` + `PackageReference GRLicence 1.1.1`), `LicenceMonitor` singleton (`GRC.API/Program.cs`), subject `/LIC/TRESO_GRC` codé en dur (absent de toute config fichier), middleware unique en amont de `UseAuthentication`/`MapControllers` lisant `GetStatus()` → `403` si invalide, service toujours démarré (fail-closed, état initial invalide). Adaptateur `AppSettingsLicenceConfigProvider` pour lire `address`/`port`/`requestTimeoutSeconds` depuis `appsettings.json` (subject absent). Décision documentée : `/api/auth/login` et `/api/reference/societes` bloqués aussi (cohérence fail-closed) ; fichiers statiques SPA (`index.html`, JS/CSS) servis avant le middleware — hors périmètre, comportement voulu (shell affiche l'erreur 403 remontée par l'API, aucune donnée métier accessible sans API). **1er REJECT** : périmètre exact du blocage insuffisamment documenté (affirmation « toutes les routes sans exception » contredite par l'ordre réel du pipeline `UseStaticFiles`/`UseDefaultFiles` avant le middleware). Corrigé : tableau route par route ajouté au VERIFY. Build 0 erreur, `GRLicence` référencé uniquement par `GRC.API` (Clean Architecture respectée) | 2026-07-20 |
| TASK-060 | Backend (API+Infra) + Front (écran rapprochement) | **Bouton « Générer règlement » sur l'écran de rapprochement bancaire** (demande PO 2026-07-16) : génère un règlement client **versement sans affectation** (`ModeNo=12`) depuis une ligne de relevé non rapprochée, résolu via `CaisseManager.ReglementCreate` (36 paramètres, mapping partagé TASK-059). Numéro via compteur officiel `GetNumeroPieceCourante`, découplé de l'auto-rapprochement (détection ultérieure par `AutoReconciliationEngine`, aucune modif `ReserverLigneAsync`/TASK-037). **1er REJECT** : `Montant`/`DateOperation`/`BanqueId` acceptés tels quels depuis le payload client, sans corrélation serveur à la ligne de relevé (`LigneReleveId` absent du DTO) — risque de règlement fantôme forgé côté client. **Corrigé** : DTO réduit à `LigneReleveId`/`ClientCode`/`CaisseCode`/`MvReference`, `ReleveBancaireRepository.GetLignePourGenerationAsync` résout Montant/Date/BanqueId à 100% côté serveur depuis `RAPP_ReleveBancaire_Ligne`/`Entete` (requête paramétrée), avec gardes ligne introuvable/déjà lettrée/crédit≤0. Revue de code confirmée point par point (contrôleur, service, repository, front). Build back/front 0 erreur | 2026-07-20 |
| TASK-063 | Front (`ReglementGenerationEspece.tsx`) | **Uniformisation de tous les filtres de l'écran génération règlements espèce en mode liste** (retour PO post-TASK-062 : `N° Facture`/`Info 3` sans valeurs proposées, clarifié en « toutes les colonnes doivent se comporter comme `Code Client` »). 10 colonnes basculées `text`/`number`/`date` → `list` (`factureNumero`, `dateFacture`, `dateEcheance`, `montant`, `solde`, `commentaire`, `info1..4`). Helper `getItemValue` unifié entre filtrage et génération d'options ; libellés formatés (dates `DD/MM/YYYY`, montants `formatMoney`) avec valeur brute conservée pour le filtrage exact ; tri intelligent par type (numérique/chronologique/alphanumérique naturel), `(Vide)` toujours en tête. Recherche du popover étendue au libellé formaté **et** à la valeur brute. Vérifié par lecture de code (`ALL_COLUMNS`, `getItemValue`, `getOptions`, `ExcelFilter.tsx`) conforme point par point au tableau colonne-par-colonne du VERIFY. Build back/front 0 erreur | 2026-07-20 |
| TASK-062 | Backend (Infrastructure) + Front | **4 corrections UX/données sur l'écran de génération règlements espèce** (retour PO post-livraison TASK-059) : `MV_Reference` transmis vide (`string.Empty`, annule le mapping round 5 de TASK-059 pour ce seul champ, `MV_Info3` inchangé) — **prouvé en base réelle `GR_GOCOM`** (`MV_Id=48411`, `MV_Reference=''`) ; colonnes `Code Client`/`Intitulé Client` séparées et filtrables indépendamment ; 2 bugs `ExcelFilter` corrigés (`localText` non resynchronisé à la réouverture, évaluation falsy de `0`/dates vides) — chaque colonne testée individuellement, filtre REPRÉSENTANT (2127→89) validé ; layout réorganisé (en-tête retiré, actions caisse/générer/total remontées dans l'en-tête du tableau), captures avant/après fournies. **1er VERIFY rejeté** (preuve base réelle absente, captures manquantes, tests filtres non documentés colonne par colonne) — corrigé et revalidé le même jour. Build back/front 0 erreur | 2026-07-20 |
| TASK-059 | Backend (API+Infra) + Front | **Génération interactive de règlement client espèce, affectation intégrale sur factures `RT_ECHEANCE`** (demande PO 2026-07-16, amendée 2026-07-20). Appel **direct** `CaisseManager.ReglementCreate` (36 paramètres, bypass `ImporterReglements`/`ReglementClientCoffreCreate`/`Verify()`). 1 règlement **par facture cochée**, jamais de split (TASK-050). **6 REJECT avant validation finale** : (1) `NullReferenceException` — `CaisseManager.SocieteManager` non résolu par Ninject, corrigé par affectation explicite. (2) VERIFY incohérent (montant erroné + législation obsolète), corrigé et revérifié. (3) Solde règlement/facture non recalculé + numéro maison au lieu du compteur applicatif — corrigé via `VerifySoldeManager.UpdateSoldeEcheance`/`UpdateSoldeReglementClient` + `SocieteManager.GetNumeroPieceCourante`. (4) `EC_Etat`/`MV_Etat` non prouvés — confirmé recalculés par le même appel `VerifySoldeManager`. (5) Test réel PO (échéance 17063/facture FAG2619813) : `RT_ECHEANCE.EC_SoldeDevise` non remis à 0 (aucune méthode DLL native ne le gère, confirmé par scan IL exhaustif 294 DLL/849k méthodes) ; `MV_Info3`/`MV_Reference` devaient utiliser `Echeance.Info3` (`EC_Info3`) avec repli `Echeance.DocumentNumero` (`DO_Numero`) au lieu de `DO_Numero` seul. (6) Clôture prématurée refusée faute de VERIFY soumis et faute d'arbitrage PO sur la dérogation à la règle « pas d'`UPDATE` SQL brut sur table pilotée par DLL ». **Dérogation PO explicitement accordée** (2026-07-20) pour l'`UPDATE` dédié sur `EC_SoldeDevise`, seul cas sans alternative DLL prouvée. **Preuves SQL réelles en base `GR_GOCOM`** : `EC_SoldeDevise` 44 961,60 €/5 705 941,51 € → 0,00 € (échéances 27534/27533, règlements 48406/48407), mapping `EC_Info3`/repli `DO_Numero` confirmé dans les deux cas. Build back/front 0 erreur | 2026-07-20 |
| TASK-053 | SQL (vue GRC) + Backend (Infrastructure) | **Écriture comptable règlement client — spec complète des 4 champs, différenciée espèce/hors espèce** (spec PO 2026-07-15, chaque affirmation vérifiée en base avant codage). Vue `vw_ReglementsAComptabiliser` : `LibelleEcriture` (`ESP <facture>` / `ESP` seul / libellé saisi / `Versement`), `ReferenceCompta` (BL espèce via `mv_info3` / réf. hors espèce), `MV_Piece` (n° règlement espèce / code banque hors espèce tronqué à 13, sans collision sur le périmètre réellement rapproché), `FactureNumero` (`RT_ECHEANCE.DO_Numero`, `OUTER APPLY TOP 1` anti-doublon sur le 1-N de `RT_AFFECTATION`). C# devenu passe-plat (`AppliquerChampsVue`), code mort supprimé (`FirstDoc`, `ChargerIntitulesModes`/`modes`). Bug de la colonne préexistante `LibelleEcritre` corrigé au passage (valait `"ESP "` seul sur 21 868/22 421 lignes espèce, jamais le n° de facture). **Testé exhaustivement sur les 46 056 lignes réelles** (contrat 1 ligne/`MV_ID`, 4 branches de libellé, troncatures, `MV_Type=4≡3`, `DocNumero1/2`, non-régression TASK-036/039). Résidu documenté : seul le passage HTTP bout-en-bout (`apercu-comptabilisation`) n'a pas été rejoué (auth indisponible) — couverture jugée suffisante (point d'injection unique + passe-plat pur), test UI de 2 min proposé au PO en recette. **Absorbe TASK-052** (sa règle de repli « mode seul » n'est jamais entrée en prod, remplacée par `Versement` porté par la vue — non archivée séparément, cf. `TODO.md`) | 2026-07-16 |
| TASK-045 | Backend (Infrastructure/API) + Front | **Filtre d'éligibilité rapprochement appliqué à tort à la liste des règlements et à la comptabilisation** — écart prod confirmé (WinForm 30 712 vs Web GRC 7 479 règlements, mêmes caisses `n.salim`). Paramètre `eligibleRappBancaire` (bool, défaut `false`) ajouté sur `GetReglements`/`GetDistinctReglements` : le filtre `EstEligibleRappBancaire` n'est appliqué **que si** `true` — seul `RapprochementBancaire.tsx` envoie ce flag. Liste et comptabilisation inchangées (tous modes, espèces incluses) ; écran de rapprochement inchangé (Virement + Chèque/Traite remis uniquement). Prouvé par revue de code exhaustive (tableau des 3 seuls appelants de l'endpoint) + build back/front 0 erreur. **Résidu non bloquant** : la mesure chiffrée exacte en prod (`n.salim`/172.16.0.205, post-déploiement du build) reste à consigner — la correction structurelle (suppression conditionnelle du filtre) est, elle, prouvée par le code | 2026-07-16 |
| TASK-050 | Backend (Infrastructure) | Lettrage auto à la comptabilisation : binding IoC `ILettrageReglementClient` (module d'origine exclu) + appel `LettrerAsync(reg)` post-compta réussie dans `Comptabiliser`, non bloquant (`lettrageWarnings` dédié). **Testé réellement en base** (`GR_GOCOM`, 4 règlements comptabilisés via l'API réelle) : lettrage confirmé côté `F_ECRITUREC.EC_Lettrage`/`EC_Lettre` pour les règlements totalement affectés, split (44517) resté non lettré comme attendu. Colonnes miroir `RT_MOUVEMENT.MV_IsLettrer` etc. jamais renseignées — **critère d'acceptation initial corrigé après clarification PO** : conforme au comportement WinForm (lettrage visible uniquement sur `F_ECRITUREC`, aucun retour attendu côté Trésorerie) | 2026-07-14 |
| TASK-048 | Backend (Infrastructure) | Collision `IEC_ECNO` en comptabilisation (DLL Sage non thread-safe) : `Parallel.ForEach` → `foreach` séquentiel dans `Comptabiliser`, collision structurellement impossible (plus de concurrence sur l'allocation DLL). **Rejoué réellement** (3 règlements, base DESKTOP-2VCUE93) : `successCount=3, errorCount=0`, `EC_No` 289671→289676 distincts et croissants, aucune `SqlException 2627` | 2026-07-14 |
| TASK-036 | Architecture | Pièce + champs compta depuis la vue SQL GRC `vw_ReglementsAComptabiliser` : `NumeroPiece` forcé à `MV_Piece` via décorateur IoC `IErpComptaService` (`DispatchProxy`, `GetNextNumero`), `Reference`/`Libelle` surchargés post-`Generate`, `DocNumero1/2` (2 col. custom, 69 car.) par `UPDATE` post-compta isolé. Date = `MV_Date` tous types. **1er REJECT** (VERIFY obsolète vs code réel — exception espèce retirée par TASK-038 non répercutée dans le VERIFY/TODO ; DocNumero1/2 et décomptabilisation non re-testés). Corrigé le même jour : doc à jour, **DocNumero1/2 rejoué en run live avec succès** (règlement 48053, `EC_Piece`/`EC_Reference`/`DocNumero1` conformes en base, aucun hang MSDTC — cause du 1er hang non identifiée avec certitude, traitée comme intermittente). Décomptabilisation **différée par décision PO** : non testable depuis GRC_WEB (action native Sage/Trésorerie, aucun endpoint exposé) | 2026-07-14 |
| TASK-047 | Backend / DLL | `NullReferenceException` DLL Sage dans `Generate` (règlement 43168, mode 18) : cause racine confirmée (décompilation + DB) — couple `(caisse, mode)` absent de `P_CAISSEMODREG`, 25 règlements concernés, systématique et non spécifique à 43168. Garde applicative `VerifierComptabilisable` ajoutée avant `Generate`, appliquée aux **deux** points d'appel (`Comptabiliser` + `ApercuComptabilisation`), aucun bypass DLL. Remédiation paramétrage documentée mais **non exécutée** (décision PO à venir) | 2026-07-14 |
| TASK-049 | Backend (Infrastructure) | Libellé écriture compta espèce (`MV_Type = 0`) = colonne `LibelleEspece` de la vue, repli mode + intitulé si vide ; branché dans `AppliquerChampsVue` (point unique aperçu + réel) | 2026-07-10 |
| TASK-001 | Sécurité | Suppression des fallbacks `sa/1234` en dur — accès via `IDbConnectionFactory` + `appsettings`/User Secrets | 2026-07-07 |
| TASK-002 | Sécurité | Garde d'accès : `FallbackPolicy = RequireAuthenticatedUser` + `[Authorize]` contrôleurs, seul `/auth/login` anonyme (corrigé après 1er REJECT) | 2026-07-07 |
| TASK-003 | Sécurité | CORS `AllowAll` → politique `RestrictedCors` (origines depuis config) | 2026-07-07 |
| TASK-004 | Métier | Validation via DLL GRC (`repo.Update`) — suppression du `UPDATE RT_MOUVEMENT` brut, `TransactionScope` + garde `IsPointe` | 2026-07-07 |
| TASK-005 | Correction | Parseur Excel : dates serial (`FromOADate`) + montants Invariant/fr-FR | 2026-07-07 |
| TASK-006 | Correction | Lignes Excel rejetées comptées et remontées (`LignesRejetees`) | 2026-07-07 |
| TASK-007 | Correction | Lettrage : reprise d'index (`GetIndexFromLettrage`, `startIndex = max+1`) | 2026-07-07 |
| TASK-008-A | Performance | Requête règlements bornée par période réelle (`dateDebut`/`dateFin`), plus de `2000→2030` systématique | 2026-07-07 |
| TASK-009 | Maintenance | DI + sortie de logique de `Program.cs` (`ReglementController`/`ReglementService`), fin du hack réflexion `_kernel` | 2026-07-07 |
| TASK-011 | Déploiement | URL API configurable (`config.js`/`window.GOCOM_CONFIG`), plus de `localhost:5044` en dur dans `src/` | 2026-07-07 |
| TASK-012 | Fonctionnel | Auto-Rapprochement branché : relevé sélectionné, vrais règlements (`MontantDeviseSociete`), propositions appliquées aux grilles | 2026-07-07 |
| TASK-013 | Correction | Lettrage manuel front : `AA` après `Z`, plus de reset arbitraire (recalcul via `useEffect`) | 2026-07-07 |
| TASK-016 | Backend/DB | Réservation persistée (Phase 1) : colonnes `ReservePar_UserId`/`DateReservation` + index unique `MV_ID`, endpoints `reserve`/`release` atomiques, `validate`→Phase 2 seule (re-vérif réservataire) ; `/reglements` remonte l'état réservé (DTO plat camelCase) — corrigé après 3 REJECT | 2026-07-07 |
| TASK-032 | Back+Front (Auth) | Compte admin (`P_UTILISATEUR.UT_Admin=1`) : claim JWT `IsAdmin` au login, bypass **serveur** du filtre caisses (→ toutes caisses société via `SELECT CA_Id WHERE SO_Id`), badge/label front. Non-admins inchangés | 2026-07-07 |
| TASK-039 | Front | Filtre caisses de la simulation compta : déroulante `caisseOptions` bornée aux caisses autorisées (`user.caisses`), **bypass admin inclus** (`user.isAdmin` → toutes caisses, cohérent avec le bypass serveur TASK-032). Pattern identique à `App.tsx`. Front only | 2026-07-10 |
| TASK-017 | Frontend | Câblage front réservation : reserve/release à l'appariement/dissociation, état survit au refresh, lignes d'autrui verrouillées (cadenas), filtre « Non rapproché » exact ; `currentUserId` = `user.no` ; lettrage réservé exposé par `/reglements` ; Phase 2 pose `DatePointage` = date valeur ISO de la ligne — corrigé après 2 REJECT | 2026-07-07 |
| TASK-019 | Frontend/UX | Filtre « Non rapprochés » 100 % session (zéro `isPointe`) ; liste GRC chargée `pointe=false` (exclusion pointé au niveau requête, bonne couche) ; paires lettrées de session non masquées et **épinglées en haut** des 2 grilles | 2026-07-07 |
| TASK-021 | Métier | Éligibilité rapprochement bancaire : helper unique `EstEligibleRappBancaire` (Virement `MV_Type=3`, ou Chèque/Traite `MV_Type∈{1,2}` **remis** `MV_Remis=2`) appliqué à l'auto-rappro (`GenererPropositions`) **et** à la grille GRC (`GetReglements`/`GetDistinctReglements`) — exclut Espèce/« Autre »/non-remis, ferme le faux match espèce↔ligne bancaire | 2026-07-07 |
| TASK-020 | Backend | `validate` 500 corrigé : suppression du `TransactionScope` multi-connexions (promotion MSDTC non supportée sous `System.Data.SqlClient`/.NET 10) ; re-check réservation en connexion fermée avant boucle DLL, pointage **par item** façon `RapprocherManuel` (`ValidationResultDto`) ; front filtre les échecs via `FailedLigneIds` — corrigé après 1 REJECT | 2026-07-07 |
| TASK-022 | Backend/DB | Colonne `DateValidation` sur `RAPP_ReleveBancaire_Ligne` posée en Phase 2 (UPDATE groupé post-boucle DLL sur les seules paires en succès, connexion ouverte après fermeture DLL — pas de MSDTC) ; `GetAllLignesExcelAsync` et `LibererLigneAsync` filtrés `DateValidation IS NULL` ; `Lettrage`/`MV_ID` conservés → ligne pushée en GRC ne réapparaît plus (symétrie `pointe=false`) | 2026-07-07 |
| TASK-024 | Backend | Endpoint `GET /api/relevebancaire/{id}/etat` : état de rapprochement d'un relevé — **toutes** les lignes (validées comprises, sans filtre `DateValidation`), statut dérivé (NonRapproche/Reserve/Valide), nom réservataire (jointure `P_UTILISATEUR`), et enrichissement règlement GRC (numéro/date/caisse/client) via DLL `Get` en lecture seule. `GetAllLignesExcelAsync`/workspace inchangés ; aucune `TransactionScope` (pas de MSDTC) | 2026-07-07 |
| TASK-025 | Frontend | « Gestion des Relevés Bancaires » (`RelevesBancaires.tsx`) : clic sur un relevé → panneau accordéon lecture seule de l'état des lignes (badges Validé/Réservé/Non traité, réservataire nom+date, règlement GRC numéro/date/caisse-libellé/client, filtre Traité/Non traité + compteurs). Caisse résolue via `caissesMap` (`/reference/caisses`, clé `c.id`) — corrigé après 1 REJECT (clé `c.no` erronée). Build front OK | 2026-07-07 |
| TASK-026 | Back+Front | Écran rapprochement : le déroulant « Relevé associé » ne liste plus que les relevés ayant ≥ 1 ligne encaissement non rapprochée (`EXISTS` sur `DateValidation IS NULL` + `Credit > 0`, lignes **réservées** incluses). Filtre **opt-in** via param `nonRapprochesSeulement` (défaut `false`) sur l'endpoint partagé → `RelevesBancaires.tsx`/TASK-025 inchangé (liste toujours tout). Lecture seule, aucun schéma modifié | 2026-07-07 |
| TASK-027 | Back+Front/UX | État du relevé : colonne unique **Montant** remplacée par **Débit / Crédit** (format bancaire). `Debit` exposé dans `LigneEtatRapprochementDto` (SQL `SELECT l.*` déjà OK, aucune modif SQL/schéma) + `debit` côté front, deux colonnes alignées à droite. Lecture seule | 2026-07-07 |
| TASK-028 | Frontend/UX | Child grid dépliable remplacé par un **écran séparé d'interrogation** du relevé (`ReleveInterrogation`) : clic sur le nom → écran plein, **filtres type Excel** par colonne (`ExcelFilter`), colonnes Débit/Crédit conservées, compteur + filtre statut conservés. Navigation par état local (`selectedReleve`), pas de router. Lecture seule | 2026-07-07 |
| TASK-031 | Correction / Métier | Validation rapprochement : `MV_Piece = MV_ExtraitNum` sans garde `Type==3` + `MV_Date = DateValeur` si `MV_Compta=0` seulement. **1er passage REJETÉ** — `reg.ChangeDate` appelé **après** `IsPointe=true` → `InvalidOperationException` (« a subi un rapprochement bancaire ») sur toutes les paires. Corrigé : `ChangeDate` avant `IsPointe`. Écriture via DLL `repo.Update`, comptabilisé jamais touché | 2026-07-08 |
| TASK-034 | Correction / Métier | Validation rapprochement : `MV_Date` / `DatePointage` / `MV_DateEcheance` calées sur la **date opération** du relevé (au lieu de la date valeur), `DateOperation` lue **côté serveur** (`RAPP_ReleveBancaire_Ligne`). Garde : `MV_Date` **et** `MV_DateEcheance` modifiées seulement si `MV_Compta=0` ; `DatePointage` toujours. `ChangeDate` avant `IsPointe`. Testé OK par le PO | 2026-07-08 |
| TASK-035 | Back+Front | Écran comptabilisation règlements clients : entrée menu **« Comptabilisation »** (composant `ApercuComptabilisation` désormais atteignable) + route déjà câblée ; DTO d'aperçu lisible `EcritureApercuDto` (mapping `EcritureComptable` → camelCase, enum `Sens`) fixant l'aperçu vide ; filtre **Rapproché** (param `pointe` existant) ; tableau enrichi (Contrepartie, Sens D/C, Échéance, Pièce* indicative). Chaîne métier `Generate`→`Comptabiliser` inchangée, aucun bypass DLL. N° pièce indicatif → TASK-036 | 2026-07-10 |
| TASK-018 | Performance/Robustesse | `GetReglements` : requête réservations `MV_ID IN @Ids` découpée en lots `Chunk(2000)` sur une **connexion unique** → plus de `SqlException` 8003 (>2100 params) sous forte volumétrie. Dictionnaire `reservations` alimenté à l'identique, ordre des règlements et résolution des noms inchangés. Lecture seule | 2026-07-10 |
| TASK-023 | UX | Cadenas de réservation : **nom** du réservataire affiché (au lieu de l'ID) aux **2 grilles** (règlements GRC + lignes relevé). Résolution `UT_Id → Nom Prénom` (repli `UT_Login` puis ID) via `P_UTILISATEUR` en une requête par flux (`GetReglements` + jointure dans `GetAllLignesExcelAsync`/`GetEtatRapprochementAsync`), exposée `reservePar_UserName` au DTO. Lecture seule, repli ID conservé | 2026-07-10 |
| TASK-029 | Correction | Écran interrogation relevé : filtres **liste** (Libellé/Code/Statut) rendus opérants — ajout du fallback `\|\| []` aux 3 `selectedValues` ([RelevesBancaires.tsx:166-170](../gocom-web/src/RelevesBancaires.tsx#L166)) qui étaient `undefined` à l'état initial et court-circuitaient `ExcelFilter`. Aucune modif backend ni d'`ExcelFilter` | 2026-07-10 |
| TASK-030 | Back+Front/UX | Liste des relevés : flèche d'interrogation obsolète retirée + **4 compteurs par relevé** (Total / Réservées / Rapprochées / Restantes sans action) via **une seule requête agrégée** (`COUNT`/`SUM(CASE)` + `LEFT JOIN`, pas de N+1) dans `GetEntetesByBanqueAsync` → `ReleveBancaireListItemDto`. Déroulant TASK-026 non régressé (flag `nonRapprochesSeulement` conservé). VERIFY conforme fourni | 2026-07-10 |
| TASK-033 | Back+Front | Suppression d'un relevé (en-tête + lignes) **uniquement si toutes les lignes sont sans action** : garde atomique `DELETE ... WHERE NOT EXISTS(Lettrage/MV_ID/DateValidation IS NOT NULL)` sous transaction + re-check `@@ROWCOUNT`, `[HttpDelete("{id}")]` → 204/409/404. Pur RAPP, **aucun appel DLL GRC** (on refuse, pas de dépointage). Front : confirmation + bouton masqué si `nbReserve + nbRapproche > 0`, message 409 affiché | 2026-07-10 |
| TASK-038 | Backend | **Pièce forcée = `MV_Piece` pour TOUS types/modes** : retrait de l'exception espèce de TASK-036 dans `PieceAForcer` → `viewRow?.MV_Piece` (repli compteur Sage `null` uniquement si le règlement est absent de la vue). Point de vérité unique : `Comptabiliser` **et** `ApercuComptabilisation` passent par `PieceAForcer` (aperçu == réel par construction). Forçage via décorateur IoC `ErpComptaPieceDecorator` (TASK-036), aucune écriture directe. Corrige l'erreur DLL Sage « unexpected caracters » (pièces à suffixe non numérique) — la vue `vw_ReglementsAComptabiliser` a été corrigée par le PO pour fournir des `MV_Piece` Sage-valides partout. Build 0 erreur. Runtime (aperçu espèce + lot mixte) à valider PO. **Dépend de TASK-036** | 2026-07-10 |
| TASK-042 | Front | **Comptabiliser depuis la liste (1 ou N) via l'écran d'aperçu — chemin unique** : le mode « Comptabiliser » de la liste route la sélection vers `ApercuComptabilisation` (prop optionnelle `preselection`, simulation auto, pas de re-fetch) ; le bouton « Comptabiliser » de l'aperçu commite `apercus.map(a=>a.id)` via `handleValider` inchangé. **Suppression du POST direct aveugle `handleSubmitComptabilisation`** → plus aucun commit sans aperçu. Mode filtres (sidebar) inchangé (`preselection` absente), gardes `hasErrors` et `isComptabilise === 0` conservées. Front only, build 0 erreur. Persistance base à valider PO (irréversible) | 2026-07-10 |
| TASK-041 | Backend (transverse) | **Journalisation fichier Serilog (1 fichier/jour)** sur réservation + approbation + comptabilisation. `Serilog.AspNetCore` + `Sinks.File`, `rollingInterval: Day` → `logs/grc-AAAAMMJJ.log` (chemin/rétention `appsettings`). Serilog **cantonné à `GRC.API`** ; Infra via `ILogger<T>` injecté (Clean Architecture). Trace entrées/décisions/résultats + **exceptions complètes** (dont échec DLL Sage avec `reglementId`), `BeginScope` de corrélation. **Additif strict** : aucun changement métier, aucun bypass DLL, aucun `UPDATE` ajouté, **aucun secret loggé**. Build 0 erreur. Runtime des 3 zones à observer côté serveur (SQL GRC + Sage réels) | 2026-07-10 |
| TASK-037 | Backend/DB/Front | **Unicité de la lettre de rapprochement** (concurrence multi-postes). Allocation de la lettre passée **côté serveur** : `/reserve` ignore la lettre client, calcule `max(présent)+1` (base 26, `LettrageGenerator` réutilisé) sous **`sp_getapplock` exclusif par relevé** dans une transaction + **UPDATE conditionnel** (`Lettrage IS NULL AND NOT EXISTS MV_ID`), renvoie la lettre attribuée. Migration `SQL_004` : **remédiation** des doublons `(EnteteId, Lettrage)` **puis** **index unique filtré** `UX_RAPP_Ligne_Entete_Lettrage` (ordre 1→2, idempotent, **aucune colonne compteur**). Front (`executeManualLettrage` + `handleAutoReconcile`) adopte `resp.data.lettrage` sur les 2 grilles ; compteur local n'est plus source de vérité. Pur RAPP, **aucune écriture GRC**. `release` inchangé. Builds 0 erreur | 2026-07-10 |
| TASK-043 | Build/Déploiement | **Build déterministe : conflit `Serilog.dll` 4.2 (NuGet, API) vs 2.10 (`libs\Tresorerie`)** révélé par TASK-041. Target MSBuild `RetirerSerilogHeriteDeLaRacine` (`AfterTargets=ResolveAssemblyReferences`) dans `GRC.API.csproj` : retire de la copie-locale **racine** les seuls `Serilog*` dont le chemin source contient `libs\Tresorerie` (jamais ceux du cache NuGet) → racine **toujours** en 4.2, 2.10 conservé au sous-dossier pour le kernel Ninject. **Aucun code C#**, version NuGet inchangée (≥ 4.2). Recette : build 0 erreur ; `publish` dossier propre → racine 4.2 + set moderne complet, `libs\Tresorerie` = 2.10 ; **déterminisme sur 2 publish** (bin/obj nettoyés) ; démarrage **sans** `FileNotFoundException Serilog 4.2`. Endpoints end-to-end non observables sur poste dev (config `.apt` Trésorerie absente — hors périmètre) | 2026-07-10 |
| TASK-046 | Backend / Perf | **Parallélisation de `ApercuComptabilisation`** (aligné sur `Comptabiliser`) : `foreach` séquentiel → `Parallel.For` degré 10, connexion + repo créés **par thread**, accumulateur thread-safe (`object[]` indexé sur la position d'entrée → **ordre de sortie préservé à l'identique**), `ComptaPieceContext.ForcedPiece` posé/nettoyé (finally) **par itération**, `viewRows`/`modes` chargés **une fois** avant la boucle. Aperçu **lecture seule** (aucune écriture GRC/Sage, pas de `Comptabiliser`/`EcrireDocNumeros`), DTO et valeurs forcées **inchangés** (iso-compta réelle), gestion d'erreur **par règlement** conservée (une ligne KO n'arrête pas le lot). `Comptabiliser` non touché. Build 0 erreur. **Gain mesuré côté PO** (log 17:30) : 5 307 règlts → **~16,5 s** (~3,1 ms/règlt), 0 échec/0 exception, soit **~6×** (avant : 4 339 → 81 s). Thread-safety confirmée runtime | 2026-07-10 |
| TASK-044 | Build/Déploiement | **Démarrage : `FileNotFoundException Serilog.Settings.AppSettings 2.0`** (révélé une fois TASK-043 passée, masqué avant par le crash 4.2). `.ReadFrom.Configuration` (Serilog.Settings.Configuration 9) scannait le `DependencyContext` et tentait de charger la brique legacy 2.0 référencée transitivement par les DLL Trésorerie (absente de la racine). Fix `Program.cs:23` : `ConfigurationReaderOptions(typeof(Serilog.ILogger).Assembly)` → **liste d'assemblies explicite**, plus aucun scan du DependencyContext. Sûr : section `Serilog` = `MinimumLevel` seul, sinks/enrichers en code (inchangés). Recette : build 0 erreur ; `publish` propre → **hôte construit entièrement** (on passe `Program.cs:23`), plus aucun `FileNotFoundException` Serilog ; arrêt ensuite sur `TresorerieGroupConfigurationValidator.Validate` (config `.apt` — env-dépendant, hors périmètre) | 2026-07-10 |
